---
title: "获取会议受邀成员列表"
source: "https://developer.work.weixin.qq.com/document/path/98160"
last_update: ""
crawl_date: "2026-02-17"
---

# 获取会议受邀成员列表

根据会议 ID 获取受邀成员列表，支持分页获取

 

**请求方式：**POST（**HTTPS**）
**请求地址：**https://qyapi.weixin.qq.com/cgi-bin/meeting/get_user_meetingid?access_token=ACCESS_TOKEN

**请求包体：**

      
```
{
	"meetingid":"meetingid11",
	"instance_id":1,
	"pos":1
}
```

    

**参数说明：**

        

| 参数 | 必须 | 说明 |
| --- | --- | --- |
| access_token | 是 | 调用接口凭证 |
| meetingid | 是 | 会议id |
| pos | 否 | 分页获取受邀成员列表的查询起始位置值。此参数为非必选参数，默认值为0，从头开始查询。输出参数“has_more”为 true，表示还存在受邀成员需要继续查询；输出参数“next_pos”即下一次查询的“pos”值。多次调用该接口直到输出参数“has_more”为 false。 |

 

**返回结果：**

      
```
{
   "errcode": 0,
   "errmsg": "ok",
   "has_more":true,
   "next_pos":2,
   "invitees":[
   {
   		"userid":"USERID",
		"name":"用户昵称"
   }
   ]
}
```

    

**参数说明：**

        

| 参数 | 说明 |
| --- | --- |
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
| has_more | 是否还有尚未拉取的成员列表 |
| next_pos | 当“has_more”为 true 时，下次查询时输入参数“pos”的值。 |
| invitees | 受邀成员列表，详见Invitee |

**Invitee说明**

        

| 参数 | 说明 |
| --- | --- |
| userid | 用户userid |
| name | 用户昵称。 |
