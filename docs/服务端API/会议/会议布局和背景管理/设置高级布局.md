---
title: "设置高级布局"
source: "https://developer.work.weixin.qq.com/document/path/98869"
last_update: ""
crawl_date: "2026-02-17"
---

# 设置高级布局

- 将会议中的高级自定义布局应用到指定成员或者整个会议。
- 恢复指定成员或整个会议的默认布局。

**请求方式：** POST（**HTTPS**）
**请求地址：** https://qyapi.weixin.qq.com/cgi-bin/meeting/advanced_layout/batch_delete?access_token=ACCESS_TOKEN
 
**请求包体：**

      
```
{
	"meetingid": "MEETINGID",
	"layout_id": "LAYOUTID1",
	"user_list": [{
		"tmp_openid": "TMPOPENID1,
		"instance_id": 1
	}]
}
```

    

**参数说明：**

        
[

****
| 参数 | 必须 | 说明 |
| --- | --- | --- |
| access_token | 是 | 调用接口凭证。获取方法查看“获取access_token” |
| meetingid | 是 | 会议 ID |
| layout_id | 否 | 选择应用的布局 ID（若送空""，表示恢复成当前会议的默认布局）。应用布局的优先级从高到低为：个性布局 > 自定义布局 > 默认布局（MRA 不支持同框模式， 如果会议设置为同框模式，MRA 应用默认布局。） |
| user_list | 否 | 用户列表对象数组。如果该字段为空，为会议设置高级自定义布局；如果该字段携带用户，则只为指定用户设置个性布局。单次最多支持20个用户。详见User |

**User 说明**

        

| 参数 | 必须 | 说明 |
| --- | --- | --- |
| tmp_openid | 是 | 用户当前会议中的临时身份 ID，单场会议唯一 |
| instance_id | 是 | 用户终端设备类型 ID：0：PSTN1：PC2：Mac3：Android4：iOS5：Web6：iPad7：Android Pad8：小程序9：voip、sip 设备10：linux20：Rooms for Touch Windows21：Rooms for Touch MacOS22：Rooms for Touch Android30：Controller for Touch Windows32：Controller for Touch Android33：Controller for Touch iOS |

**返回结果：**

      
```
{
	"errcode": 0,
	"errmsg": "ok"
}
```

    

**参数说明：**

        

| 参数 | 说明 |
| --- | --- |
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
